// import { prisma } from "@/services/prisma";

export async function ViewPost(session: any) {
  // if (session !== undefined && session !== null) {
  //   const userData = await prisma.user.findUnique({
  //     where: {
  //       email: session.user.email,
  //     }
  //   })

  //   console.log(userData);
  // }
  return ''


}